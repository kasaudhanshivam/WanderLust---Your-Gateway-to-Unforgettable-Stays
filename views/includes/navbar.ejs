<style>
  .search-btn{
    background-color: #fe424d;
    color: #fff;
    border-radius: 25px;
    padding: 0 1rem 0 1rem;
  }

  .search-btn:hover{
    background-color: #fe424d;
    color: #fff;
  }
  .search-btn i{
    display: inline;
    margin-right: 0.5rem;
  }
  .search-inp{
    border-radius: 25px;
    border-radius: 0.5rem 3rem 0.5rem 3rem;
    font-size: 1rem;
  }

/* .navbar-brand{
  font-size: 1.75rem;
  font-weight: 500;
  color: #fe424d;
}
.navbar-brand:hover{
  font-size: 1.75rem;
  font-weight: 500;
  color: #fe424d;
} */

.navbar-nav .nav-link{
  border-radius: 1rem;
  padding-left: 1rem;
}
.navbar-nav .nav-link:hover{
  background-color: rgb(226, 226, 226);
}




/* Default navbar background */
 .navbar {
    background-color: #f8f9fa;
    transition: background-color 0.3s ease;
  }

  /* Hide background behind menu items on mobile when collapsed */
  @media (max-width: 991px) {
    .navbar-collapse {
      background-color: #f8f9fa !important; /* Ensure background is visible when expanded */
      /* padding: 0; Remove any padding when collapsed */
    }

    .navbar-nav {
      background-color: #f8f9fa; /* Add background color to the menu to prevent overlap */
      padding: 1rem; /* Ensure menu has space on collapsed view */
      border-radius: 5px; /* Optional: Add a border radius to the menu */
    }

    .navbar-toggler-icon {
      background-color: transparent; /* Optional: makes the toggle button transparent */
    }
  }



</style>


<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="/listings"><i class="fa-regular fa-compass"></i></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
     <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-link" href="/listings">Explore</a>
        </div>
        <div class="navbar-nav ms-auto">
          <form class="d-flex" role="search">
            <input class="form-control me-2 search-inp" type="search" placeholder="Search Destinations">
            <button class="btn search-btn" type="submit"><i class="fa-solid fa-magnifying-glass"></i>Search</button>
          </form>
        </div>
        
        <div class="navbar-nav ms-auto">
          <a class="nav-link" href="/listings/new">WanderLust your home</a>
          <%if(!currUser) { %>
          <a class="nav-link" href="/signup"><b>SignUp</b></a>
          <a class="nav-link" href="/login"><b>Login</b></a>
          <% } %>
          <%if(currUser){ %> 
          <a class="nav-link" href="/logout">Logout</a>
          <% } %>
        </div>


      </div>
    </div>
  </nav>
